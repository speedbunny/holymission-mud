SYNOPSIS
	string query_ip_name(object ob)

DESCRIPTION
	Give the ip-name for player "ob". An asynchronous process 'hname' is
	used to find out these names in parallel. If ther are any failures to
	find the ip-name, then the ip-number us returned instead.

SEE ALSO
	query_ip_number(E)
