string regexp(list,pattern)
string list,pattern

Match the pattern pattern against all strings in list, and return a new array
with all strings that matched. Special characters are:

. ^ $ & * ? < > { } |

See the manual page of the UNIX command egrep for more information.
