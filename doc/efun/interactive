SYNOPSIS
	int interactive(object ob)

DESCRIPTION
	Return non-zero if "ob" is an interactive player. 0 will be returned
	if he is link dead.

SEE ALSO
	query_ip_number(E), query_ip_name(E), query_idle(E)
