SYNOPSIS
	object *deep_inventory(object ob);

DESCRIPTION
	Returns an array of the objects contained in the inventory of <ob> and
	in the inventories of these objects, climbing down recursively.

	 ob
	  ob1
	  ob2
	    ob2A         -> ({ob1, ob2, ob3, ob2A, ob2B, ob2C, ob2Ba})
	    ob2B
	      ob2Ba
	    ob2C
	  ob3

SEE ALSO
	first_inventory(E), next_inventory(E), all_inventory(E)
