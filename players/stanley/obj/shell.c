inherit "obj/armour";
reset(arg) {
   set_name("shell");
   set_alias("shield");
   set_short("Turtle shield");
   set_long("A shield fashioned from a tortoise shell.\n");
   set_type("shield");
   set_value(420);
   set_weight(1);
   set_ac(1);
}
