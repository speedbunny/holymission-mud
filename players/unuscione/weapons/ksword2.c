  inherit "obj/weapon";
reset(arg) {
  ::reset(arg);
if(arg) return;
   set_name("hoopak");
   set_value(400);
   set_class(8);
   set_weight(4);
   set_short("A maple hoopak staff");
   set_long("A forked staff-sling...for kender warriors\n");
   }
