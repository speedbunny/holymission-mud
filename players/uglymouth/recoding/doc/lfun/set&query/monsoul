SET&QUERY - monsoul.c

set_cap_name(): This generates the name with a capital. This is used for
                internal purposes only.
query_brief(): This returns if the monster is in brief mode (1) or verbose
               mode (0).
query_prevent_shadow(): This returns 1, and is used to prevent the adding of
                        shadows to this object.
