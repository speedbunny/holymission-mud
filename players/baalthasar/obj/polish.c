inherit "obj/treasure";
reset(arg) {
if (arg) return;
set_id("polish");
set_alias("can");
set_short("A can of polish");
set_long("The can's missing the lid, and the polish seems to have almost run out.\n");
set_value(5);
  }
