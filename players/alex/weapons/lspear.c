inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("long spear");
    set_alias("spear");
    set_short("A long spear");
    set_long("A long spear it looks very deadly . \n");
    set_class(8);
    set_weight(1);
    set_value(1000);
}
