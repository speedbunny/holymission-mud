inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("short spear");
    set_alias("spear");
    set_short("A short spear");
    set_long("A short spear from the castle of Cathas.\n");
    set_class(6);
    set_weight(1);
    set_value(350);
}
 
