inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("Lepords claw");
    set_alias("claw");
    set_short("A Leapords claw");
    set_long("A Lepord's claw from a pet snow lepord. \n");
    set_class(9);
    set_weight(1);
    set_value(950);
}
 
 
 
