inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("Guard sword");
    set_alias("sword");
    set_short("A guard sword");
    set_long("A short but fast sword used by Cathan guards. \n");
    set_class(10);
    set_weight(2);
    set_value(300);
}
 
 
 
 
 
 
 
 
