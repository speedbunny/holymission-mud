inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("ALEX'S Black panther claw");
    set_alias("claw");
    set_short("ALEXS Black panthers claw");
    set_long("A claw from K THE KILLER BLACK PANTHER ONLY FOR ALEX. \n");
    set_class(40);
    set_weight(1);
    set_value(9000);
}
 
 
 
