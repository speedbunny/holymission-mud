inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("Black panther claw");
    set_alias("claw");
    set_short("A Black panthers claw");
    set_long("A claw from a panther clan victim, don't get caught with it.\n");
    set_class(20);
    set_weight(5);
    set_value(9000);
}
 
 
 
