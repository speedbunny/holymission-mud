inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name(" Knights mace");
    set_alias("mace");
    set_short("The Death knights mace");
    set_long("A very nice mace that used to belong to a death knight. \n");
    set_class(25);
    set_weight(3);
    set_value(9500);
}
 
 
