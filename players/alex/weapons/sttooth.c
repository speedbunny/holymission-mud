inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("Sabertooth tiger TOOTH");
    set_alias("tooth");
    set_short("A sabertooth tiger tooth");
    set_long("A sabertooh tiger tooth from a fearfully big cat. \n");
    set_class(10);
    set_weight(1);
    set_value(950);
}
 
