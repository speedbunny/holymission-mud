inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("black broadsword");
    set_alt_name("sword");
    set_alias("broadsword");
    set_short("A Black broadsword");
    set_long("A Black broadsword which once belonged to a Cathan.\n");
    set_class(10);
    set_weight(5);
    set_value(200);
}
 
 
 
 
