inherit "/obj/weapon";
 
reset(arg) {
    if(arg) return;
    set_name("A guards mace");
    set_alias("mace");
    set_short("A guards mace");
    set_long("A guards mace blunt but deadly.\n");
    set_class(9);
    set_weight(1);
    set_value(450);
}
 
 
 
 
 
 
