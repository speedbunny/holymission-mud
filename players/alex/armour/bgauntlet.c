inherit "obj/armour";
 
reset(arg) {
  ::reset(arg);
 
    if(arg) return;
    set_name("Black gauntlets");
    set_alias("gauntlets");
    set_short ("A set of black gauntlets");
    set_long ("A set of black plate gauntlets.\n");
    set_type ("gauntlets");
    set_ac(1);
    set_weight(1);
    set_value(300);
}
 
 
