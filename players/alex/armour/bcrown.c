inherit "obj/armour";
 
reset(arg) {
  ::reset(arg);
    if(arg) return;
    set_name("Highlord's Alex crown");
    set_alias("crown");
    set_alt_name("bcrown");
    set_short ("A Highlords crown");
    set_long ("A crown which was given to the last guard as he lay dead at Alex's feet.\n");
    set_type ("Crown");
    set_ac(1);
    set_weight(1);
    set_value(2000);
}
 
