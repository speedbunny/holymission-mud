string *feelingInfo() {
    return ({
        "$Ns somehow manages to drive $Ps knee into $Ps groin and double$s \
over in pain.",
        "$Ns raise$s $Ps knee into $Qv groin, sending $Ov sprawling to the \
floor in pain.",
        0,
    });
}
