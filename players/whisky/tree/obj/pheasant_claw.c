inherit "obj/weapon";

  reset(arg) {
  if (arg) return;

   set_id("pheasant claw");
   set_alias("claw");
   set_short("A pheasant claw");
  set_long("The sharp weapon of the pheasant.\n");
  set_type(2);
   set_class(6);
   set_value(20);
   set_weight(1);

  } 
