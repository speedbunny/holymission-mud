inherit "obj/weapon";

  reset(arg) {
  if (arg) return;

  set_id("eagle claw");
   set_alias("claw");
  set_short("An eagle claw");
  set_long("The dangerous weapon of an eagle.\n");
  set_type(2);
  set_class(14);
  set_value(400);
   set_weight(1);

  } 
