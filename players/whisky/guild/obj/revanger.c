inherit "obj/treasure";

/* this should be a possibility to revange if you got killed by a high level */
/* it should work for every guild , but should cost some money */

reset (arg) {
  if (arg) return;
    set_id("revanger");
