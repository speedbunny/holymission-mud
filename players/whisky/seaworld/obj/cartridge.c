inherit "/obj/treasure";
reset(arg) {
if(arg) return;
    set_id("cartridge");
    set_alias("inc cartridge");
    set_short("An inc cartridge");
    set_long("A little inc cartridge from the cuddlefish.\n");
   set_value(50);
    set_weight(1);
}

