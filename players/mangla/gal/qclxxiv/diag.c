/* empty file till the old file is recoded suitable for the lib */
inherit "/obj/treasure";

reset(int arg) {

    set_name("Diagnoser");
    set_short("Diagnoser");
    set_long("This item is not working right now.\n");
    set_alias("diagnoser");
    set_weight(1);
    set_value(5);
}

