inherit "obj/armour";
void reset(int arg){
  ::reset();

    set_name("medallion");
    set_alias("#QMED#");
     set_short("Guard's Medallion");
    set_long(
"The medallion is worn by all of the castle guards in Bismarck Keep.  Each "+
"one of them is the same, they are worn to prove membership in the guard. "+
"It is made of solid gold and a picture of Count Bismarck is carved into it.\n");
    set_value(100);
    set_weight(1);
    set_type("necklace");
  }
