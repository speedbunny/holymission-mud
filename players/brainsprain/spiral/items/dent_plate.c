inherit "obj/armour";
void reset(int arg) {
  ::reset(arg);

    set_name("platemail");
    set_alias("platemail");
    set_short("Dented platemail");
    set_long(
"This platemail is covered with chops.  There is brown stains on the leather "+
"  straps that barely hold this armor together.  A massive dent is in the "+
"  center of the chestpiece.  A greenish fluid oozes from some of the seams.\n");
    set_value(150);
    set_weight(5);
    set_type("armour");
    set_ac(3);
  }

