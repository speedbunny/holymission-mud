inherit "/obj/container";

reset(arg) {
  ::reset(arg);
  set_name("bowl");
  set_short("A soup bowl");
  set_long("A soup bowl for the Druid Lakmir's favourite food.\n");
  set_weight(1);
  set_value(10);
}
