inherit "/obj/treasure";

reset(arg){

   if (arg)return;
    set_id("eggshell");
    set_alias("eggshell");
    set_short("A bit of eggshell");
    set_long("This is a bit of eggshell that only the fairies, ellyllons know how to ride.\n");
    set_value(20);
    set_weight(1);
}
