inherit "/obj/treasure";

reset(arg){

   if (arg)return;
    set_id("a daisy");
    set_alias("daisy");
    set_short("A beautiful, yellow and white colored daisy");
    set_long("A beautiful, yellow and white colored daisy to give to your heart's desire.\n");
    set_value(20);
    set_weight(1);
}
