inherit "/obj/treasure";

reset(arg){

   if (arg)return;
    set_id("a crocus");
    set_alias("crocus");
    set_short("A beautiful, golden colored crocus");
    set_long("A beautiful, golden colored crocus to give to your heart's desire.\n");
    set_value(20);
    set_weight(1);
}
