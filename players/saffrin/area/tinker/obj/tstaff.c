inherit "obj/weapon";

reset(arg){

  ::reset(arg);
    set_name("staff");
    set_short("A tinker staff");
    set_long("An ash staff with jade stones embedded in it.\n");
    set_class(7);
    set_weight(1);
    set_value(141);
}

